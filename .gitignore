# Built example binaries / objects
examples/refl
examples/serialize_poc
*.o
*.so
*.dylib
.DS_Store

# Repo-local build state (recreate from scratch via CLAUDE.md)
build/
.venv/

# Repo-local LLVM toolchain (built from the llvm-project submodule)
toolchain/
toolchain-build/
toolchain-build.log
