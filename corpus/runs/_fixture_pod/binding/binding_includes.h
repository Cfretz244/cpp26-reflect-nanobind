// Plain C++ includes for the POD fixture -- parsed by BOTH the reflection
// toolchain (generator / constexpr lane) and the production compiler (the
// emit lane's generated TU).
#pragma once
#include "pod_fixture.h"
