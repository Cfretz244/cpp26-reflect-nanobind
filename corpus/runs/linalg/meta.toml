# Per-run descriptor for sgorsten/linalg (v2.2). Consumed by corpus/lib/run_gates.py.
slug         = "linalg"
tier         = 0
header_only  = true
url          = "https://github.com/sgorsten/linalg"
pin          = "v2.2"                  # commit 4460f1f
include_root = "../../libs/linalg"     # relative to this run dir; the submodule
main_headers = ["linalg.h"]
module_name  = "linalg_ext"
strategy     = "single_stage"

# Gate 2 record.
reflect_args     = ["^^linalg::vec<float,3>"]
subset_rationale = "linalg is entirely templates; vec<float,3> is the only concrete auto-bindable surface (data x/y/z, ctors, operator[]). Free-function API is function templates (not auto-bound)."
skipped_features = ["free-function templates (dot/length/normalize/...)", "mat<> types"]
