// Plain C++ includes for the tinyobjloader run (both compilers). The
// TINYOBJLOADER_IMPLEMENTATION single-TU define is NOT made here: each
// consumer that must carry the implementation (binding.cpp, the generated
// emit TU's preamble) defines it before including; the emit GENERATOR only
// reflects and never links the implementation symbols.
#pragma once
#include "objtest.h"
